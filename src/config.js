const path = require('path')

const TEMP_FOLDER = path.resolve(__dirname, '..','assets', 'temp')
const CACHE_FOLDER = path.resolve(__dirname, '..','cache')
const PREFIX = '/'
const BOT_EMOJI = '(ºº)'
const BOT_NAME = 'EfsAutomatter'
const ADMIN_JID = "5512920003537"

module.exports = {
    TEMP_FOLDER,
    CACHE_FOLDER,
    PREFIX,
    BOT_EMOJI,
    BOT_NAME,
    ADMIN_JID
}